# Home Assistant base config — GitOps managed, mounted read-only at /config/configuration.yaml.
# Integration credentials are NOT stored here; they live in /config/.storage on the PVC.
default_config:

homeassistant:
  name: Home Assistant
  time_zone: Europe/Madrid
  unit_system: metric
  country: ES
  currency: EUR
  external_url: https://homeassistant.pascualgrau.com
  internal_url: http://192.168.0.25:8123
  packages: !include_dir_named packages

http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 127.0.0.1
    - ::1
    - 192.168.0.25 # k3s node (host-network path)
    - 192.168.0.35 # Cilium Gateway LoadBalancer
    - 10.42.0.0/16 # k3s pod CIDR (cloudflared)
  ip_ban_enabled: true
  login_attempts_threshold: 5

recorder:
  auto_purge: true
  purge_keep_days: 10
  commit_interval: 30
  exclude:
    domains:
      - updater
    entity_globs:
      - sensor.*_last_seen
      - sensor.*_linkquality
      - sensor.*_signal_strength

logger:
  default: warning

frontend:
  themes: !include_dir_merge_named themes

automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
