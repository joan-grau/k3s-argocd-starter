apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: doppler
  namespace: n8n
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            name: doppler-token
            key: dopplerToken
      nameTransformer: lower-kebab
