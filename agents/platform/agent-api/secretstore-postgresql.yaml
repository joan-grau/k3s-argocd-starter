apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: doppler-postgresql
  namespace: agent-api
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            name: doppler-token-postgresql
            key: dopplerToken
