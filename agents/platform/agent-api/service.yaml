apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: agent-api
  labels:
    app: agent-api
spec:
  selector:
    app: agent-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
