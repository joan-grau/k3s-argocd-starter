apiVersion: v1
kind: ResourceQuota
metadata:
  name: agent-api-quota
  namespace: agent-api
spec:
  hard:
    requests.cpu: "100m"
    requests.memory: "512Mi"
    limits.cpu: "600m"
    limits.memory: "1024Mi"
