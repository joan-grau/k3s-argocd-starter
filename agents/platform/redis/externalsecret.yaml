apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: redis-credentials
  namespace: redis
spec:
  secretStoreRef:
    name: doppler
    kind: SecretStore
  target:
    name: redis-credentials
  refreshInterval: 1h
  dataFrom:
    - find:
        name:
          regexp: ".*"
