apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:

  # Gateway resources
  - gateway-class.yaml
  - ns.yaml
  - gw-internal.yaml
  - cert-local.yaml


generatorOptions:
  disableNameSuffixHash: true