apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: main-pool
spec:
  blocks:
    - cidr: "192.168.0.35/32"  # Gateway LoadBalancer IP
    - cidr: "192.168.0.53/32"  # AdGuard Home DNS IP
  allowFirstLastIPs: "Yes"
