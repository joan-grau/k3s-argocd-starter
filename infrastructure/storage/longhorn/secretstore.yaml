apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: doppler
  namespace: longhorn-system
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            name: doppler-token
            key: dopplerToken
